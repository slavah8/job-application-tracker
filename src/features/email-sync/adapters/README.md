# Email Sync Adapters

This folder is reserved for provider-specific adapters in a future step.

Examples:

- `yahoo-adapter.ts`
- `gmail-adapter.ts`
- `imap-adapter.ts`

The goal is to keep provider logic separate from the main job tracker domain.
